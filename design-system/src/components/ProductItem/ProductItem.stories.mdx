import {
  ArgsTable,
  Meta,
  Canvas,
  Story,
} from "@storybook/addon-docs/blocks";
import { config, withDesign } from "storybook-addon-designs";
import ProductItem from ".";
import Button from "../Button";
import Icon from "../Icon";
import productImage from "./productImageSample.jpg";


<Meta title="Components/ProductItem" parameters={{
    viewport: {
      defaultViewport: 'mobile1'
    },
    design: config({
        type: "figma",
        url:
          "https://www.figma.com/file/AWWkZAdBsBvZJHu6PnjbtP/Workshop-design-file-1?node-id=264%3A397",
      }),
  }}/>

export const ProductItemArgs = {
  name: "Delicious Vanilla Cupcake",
  image: productImage,
  price: "€2.00",
  description: "Sweets",
};

## ProductItem

ProductItem component displays product name, image and price.

<Canvas>
  <Story name="Small" args={ProductItemArgs} >
    {(args) => <ProductItem size="small" {...args} />}
  </Story>
  <Story name="Medium" args={ProductItemArgs}>
    {(args) => <div style={{maxWidth: "140px"}}>
<ProductItem size="medium" {...args} />
</div>
}
  </Story>
  <Story name="Big" args={ProductItemArgs}>
    {(args) => <div style={{maxWidth: "295px"}}><ProductItem size="big" {...args} /></div>}
  </Story>
</Canvas>


## Props

<ArgsTable of={ProductItem} />
